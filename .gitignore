# Secrets — never commit. Use .env.example as the template.
.env
.env.*
!.env.example
*.key
*.pem

# Local dev artifacts
_harness.html
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
dev/.indexnow-key

# Built from ops/console.html by dev/build-static.py. Internal tooling —
# never published, and regenerated on every build.
ops/console.built.html
ops/*.json
